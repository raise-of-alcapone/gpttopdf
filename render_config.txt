# Render.com Build Configuration
# https://render.com/docs/configure-environment

# Python version
python_version = "3.13.4"

# Build command
build_command = "chmod +x install.sh && ./install.sh"

# Environment variables for wkhtmltopdf
WKHTMLTOPDF_CMD = "/usr/bin/wkhtmltopdf"

# For Render.com, we may need to use system packages
# Add these to the Render.com dashboard if needed:
# - wkhtmltopdf
# - xvfb
# - fonts-noto-color-emoji
# testeste4